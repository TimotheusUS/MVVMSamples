<Window x:Class="TimotheusUS.MVVMsamples.Tree.ViewMain"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Views="clr-namespace:TimotheusUS.MVVMsamples.Tree"
        xmlns:ViewModels="clr-namespace:TimotheusUS.MVVMsamples.Tree"
        Title="MainWindow" Height="700" Width="525">
    <Window.DataContext>
        <ViewModels:ViewModelMain />
    </Window.DataContext>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_DBCs" >
                <MenuItem Header="_Open"  />
            </MenuItem>
        </Menu>
        <Views:ViewProjectManager DockPanel.Dock="Left" DataContext="{Binding ViewModelProjectManager1}" />
        <Views:View2 DockPanel.Dock="Top" DataContext="{Binding ViewModel2}" />
    </DockPanel>
</Window>
